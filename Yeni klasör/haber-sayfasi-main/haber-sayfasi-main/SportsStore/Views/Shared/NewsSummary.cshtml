@model HaberleriEkleModelClass
@*<div class="card card-outline-primary m-1 p-1">
        <div class="bg-faded p-1">
            <h4>
                @Model.Baslik
                <span class="badge badge-pill badge-primary" style="float:right">
                    <small>@Model.Ozet</small>
                </span>
            </h4>
        </div>
            <input type="hidden" asp-for="NewsID" />
            <span class="card-text p-1">
                @Model.Icerik
            </span>
            <span class="card-text p-1">
                @Model.HaberlerinKategorisi
            </span>
    </div>*@
<div>
    <div class="card col-12 mt-3 mb-3">
        <img src="@Model.ResimYol" class="card-img-top" style="width: 1000px;" alt="@Model.Baslik">
        <div class="card-body">
            <h5 class="card-title">@Model.Baslik</h5>
            <p class="card-text">@Model.Ozet</p>
            <a href="#" class="btn btn-primary">Detaylar</a>
        </div>
    </div>
</div>
